{
  "name": "@asii/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "test": "echo \"No tests in shared\"",
    "test:unit": "echo \"No unit tests in shared\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "env:check": "node -e \"console.log('No env vars to check for shared')\"",
    "clean": "rm -rf dist .turbo"
  }
}
